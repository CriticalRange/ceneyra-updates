import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  // GitHub Pages serves from /ceneyra-updates subdirectory
  basePath: "/ceneyra-updates",
};

export default nextConfig;
